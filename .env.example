# Connection string do Postgres (local: docker compose up -d | prod: Neon)
DATABASE_URL=postgres://futzenha:futzenha@localhost:5433/futzenha

# Senha do painel /admin
ADMIN_PASSWORD=troque-esta-senha

# Segredo para assinar o cookie de sessao (qualquer string longa e aleatoria)
SESSION_SECRET=troque-por-uma-string-aleatoria-longa

# URL publica do site (usada nos links de convite e nos metadados)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
